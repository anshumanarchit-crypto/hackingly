# ProofMesh: Offline Evidence-Bound RAG with True Hybrid Retrieval
sentence-transformers>=3.0.0
rank_bm25>=0.2.2
numpy>=1.24.0
pytest>=7.0.0
requests>=2.28.0
ollama>=0.1.0

# Feature 3: PII Vault & Privacy Protection
pydantic>=2.0.0
fastapi>=0.100.0
# Optional: spacy>=3.7.0 (en_core_web_sm) for augmented NER; fallback regex heuristics used if absent
# Optional: reportlab>=4.0.0 for PDF exports; plain-text export used if absent
